# This container will use HTU21D

version: '3.8'

services:
  htu21d:
    container_name: htu21d
    build: .
    volumes:
      - /sys/class/gpio
    deploy:
      mode: global
      placement:
        constraints:
          - node.labels.project==
