version: '3.8'
services:
  ubuntuDef: # Standard Ubuntu Image
    #image: 326567/ubuntu_default:v7
    build:
      context: /
      dockerfile: /Dockerfile
    container_name: ubuntuv7
    restart: on-failure
    deploy:
      mode: replicated
      placement:
        constraints:
          - node.labels.?==?