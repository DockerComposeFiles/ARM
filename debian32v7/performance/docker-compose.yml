version: '3.9'
services:
  performance: # Berechnungen der Geschwindigkeiten
    build:
      ./Dockerfile
    image: 326567/debian:p1
    container_name: deb-performance
    deploy:
      placement:
        constraints:
          - node.labels.test==performance