version: "3.3"
services:
  gpio-BMP280:
    container_name: BMP280
    build:
      context: ./
      dockerfile: ./Dockerfile
    devices:
      - /dev/gpiomem
      - /dev/i2c-1
    deploy:
      placement:
        constraints:
          - node.hostname==R3