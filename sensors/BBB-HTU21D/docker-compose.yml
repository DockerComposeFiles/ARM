version: '3.8'

services:

  bbb_i2c_htu21d:
    container_name: bbb_i2c_htu21d
    build:
      context: .
      dockerfile: Dockerfile
#    privileged: true
    devices:
      - /sys/class/gpio
      - /dev/i2c-0
      - /dev/i2c-1
